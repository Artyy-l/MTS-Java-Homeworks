spring:
  profiles:
    active: test

app:
  security:
    jwt:
      issuer: "todolist-test"
      secret: "test-secret-for-hometask-4"
      expiration: 30m
    password:
      pepper: "test-pepper"
